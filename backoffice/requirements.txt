# Flask
Flask==3.1.0
Flask-SQLAlchemy==3.1.1

# Auth
Authlib==1.4.1
requests>=2.32.0

# Image processing
Pillow==11.1.0

# Turso / libSQL
# libsql-experimental: driver nativo SQLite-compatibile (richiede Python <=3.12 e wheel pre-compilata)
libsql-experimental==0.0.55
# libsql-client: client HTTP puro Python per query dirette a Turso (nessuna compilazione Rust)
libsql-client==0.3.0

# Production server
gunicorn==23.0.0

# Env
python-dotenv==1.1.0
